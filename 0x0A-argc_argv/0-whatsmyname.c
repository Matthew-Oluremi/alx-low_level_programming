#include <stdio.h>

/**
 * main - main program that prints it name followed by a newline.
 * @argc: argument count.
 * @argv: argument vector.
 *
 * Return: retun 0.
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
