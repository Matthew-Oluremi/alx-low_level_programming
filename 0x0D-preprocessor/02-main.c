#include <stdio.h>

/**
 * main - Main program that prints the name of the file
 * it was compiled from, followed by a new line.
 *
 * Retun: Returns 0 (Success).
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
